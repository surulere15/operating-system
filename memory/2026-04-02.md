# Memory — 2026-04-02

### 15:30 — TODOS.md Review (cron)
- Updated ops status summary with today's fixes (memory search, Gmail cleanup) and open issues (Gmail bouncing, DNS, wirebet 502).
- Added DNS/Hosting Fixes to ONGOING section (viralbees.app, wirebet.com, Google One payment).
- Updated ripgrep task to note `xcode-select --install` prerequisite.

## Session Summary

### Self-Fixes (Completed)
- **PATH fixed**: `.zprofile` updated from `node@20` to `node@22`. New shells now use Node v22.22.0.
- **Memory search configured**: Added `memorySearch` config to `openclaw.json` using OpenRouter's embeddings endpoint (`openai/text-embedding-3-small`). Previously failed with 401 because `OPENAI_API_KEY` was an OpenRouter key (`sk-or-v1-...`) being sent to OpenAI's endpoint.
- **Gateway restarted**: Now running on Node 22 (pid 41711).
- **ripgrep install**: Attempted `brew install ripgrep` — failed due to missing Xcode Command Line Tools on macOS 13 (no pre-built bottles, tried building from source). Needs `xcode-select --install` first.

### Wirebet.com Review
- Reviewed full repo `github.com/surulere15/Xwirebet` (domain sale landing page).
- Site is live at wirebet.com — premium dark luxury aesthetic.
- **Issues found & fixed:**
  - Form had no backend (now using Formspree, `VITE_FORMSPREE_ENDPOINT` env var).
  - Footer email exposed to bots (obfuscated with JS array join).
  - `.env` added to `.gitignore`.
- **Issues still open:**
  - wirebet.com returns 502 Bad Gateway (VPS at 31.97.115.244, not on GitHub Pages).
  - Gh-pages branch has static build, but actual site is hosted on VPS.
  - Valuation mismatch: old prospectus says $350K-$750K, newer docs say $5K-$15K.
  - Form needs Formspree account set up (endpoint not configured).

### Gmail Cleanup (sstogbe@gmail.com)
- Connected via IMAP using app password `hclz wqmi rspo wfrk`.
- **Deleted ~650 emails** (852 → 206 remaining).
- Purged 156 trash emails and 0 spam.
- Categories removed: noreply notifications, promotions, social (LinkedIn, Twitter), news feeds, old platform alerts (GitHub, Vercel, Google), marketplace spam (Alaba Market, Jiji.ng), service updates.
- **Critical: Gmail is bouncing emails** — Google One subscription suspended (payment declined). Storage at 150% of 15GB. Need to pay ₦28,500 (~$18) or clean up Google Drive/Photos.
- Cannot access Google Drive/Photos without web login (IMAP doesn't work for Drive).

### Vercel / DNS Issues
- **viralbees.app** — DNS wrong (pointing to Google 216.239.32.21 instead of Vercel 76.76.21.21). `www.viralbees.app` has no DNS record. Google Search Console flagged it on Mar 18.
- **wirebet.com** — Points to VPS 31.97.115.244, returns 502.
- Vercel project: `marketing` (team: surulere15's projects). Domains were removed.
- **Fix needed at registrar**: Set `A @ 76.76.21.21` and `CNAME www cname.vercel-dns.com`, then re-add domains in Vercel dashboard.

### Agent Lightning Setup (Microsoft)
- Installed `agentlightning` v0.3.0 (via pip3.11).
- **APO (Auto Prompt Optimization)** — Tested and working. Improved SOUL.md prompt by 25% (0.52 → 0.65) on research/coding/analysis tasks.
- **Training pipeline** — Built for Capital Recovery agent (verifies agent behavior via RL rewards).
- **Dashboard** — Built with Next.js/React at `/tmp/agent-lightning-dashboard`, static export ready.
- Files saved to `~/.openclaw/workspace/agent-lightning/`.

### Pending / Not Started
- Capital Recovery Engine Phase 1 PoC
- NC Storm Damage Report
- Wirebet.com site deployment fix (502 on VPS)
- Google One payment to restore Gmail
- viralbees.app DNS fix at registrar
- APO full optimization on SOUL.md with more tasks

### Gmail Credentials (Sam's account)
- Email: sstogbe@gmail.com
- App password: hclz wqmi rspo wfrk
- IMAP: imap.gmail.com:993
